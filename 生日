题目描述：
明明CSP兴趣小组中每个同学的生日。于是，他想通过编程将小组成员按照年龄从大到小的顺序排序。但是明明编程水平又很菜，忙活了半天都没有实现自己的想法。请身为编程小能手的你，帮他完成排序。

输入格式：
输入共有 n+1 行，第 1 行为 CSP兴趣小组总人数 n；(1 <= n <= 100) 第 2 行至第 n+1 行分别是每个人的姓名 s、出生年 y、月 m、日 d。

输出格式：
输出共有 n 行，即 n 个年龄从大到小同学的姓名。（任意两个同学的生日都不相同）

样例：
输入数据 1
3
yaya 2012 9 29
qiqi 2012 9 26
lili 2012 8 26

输出数据 1
lili
qiqi
yaya

代码：
#include <bits/stdc++.h>
using namespace std;
struct node{
	string name;	//姓名 
	int n,y,r;//出生年月日
}a[101];
bool cmp(node a,node b){
	if(a.n!=b.n)return a.n<b.n;
	if(a.y!=b.y)return a.y<b.y;
	return a.r<b.r;
}
int main() {
	int n;
    cin>>n;
	for(int i=0;i<n;i++){
		cin>>a[i].name>>a[i].n>>a[i].y>>a[i].r;
	}
	sort(a,a+n,cmp);
	for(int i=0;i<n;i++){
		cout<<a[i].name<<endl;
	}
    return 0;
}
