奇怪的电梯：

题目描述：
大楼的每一层楼都可以停电梯，而且第 i 层楼 （1≤i≤N）上有一个数字 Ki（0≤Ki≤N）。电梯只有四个按钮：开，关，上，下。上下的层数等于当前楼层上的那个数字。当然，如果不能满足要求，相应的按钮就会失灵。例如：3 3 1 2 5 代表了 Ki（K1=3,K2=3,...），从一楼开始。在一楼，按“上”可以到 4 楼，按“下”是不起作用的，因为没有 −2楼。 那么，从 A 楼到 B 楼至少要按几次按钮呢？

输入格式：
输入共有二行。 第一行为三个用空格隔开的正整数，表示 N,A,B (1≤N≤200,1≤A,B≤N)。 第二行为 N 个用空格隔开的正整数，表示 Ki。

输出格式：
输出仅一行，即最少按键次数，若无法到达，则输出 −1。

样例：
输入数据 1：
5 1 5
3 3 1 2 5

输出数据 1：
3

代码：
#include <bits/stdc++.h>
using namespace std;
// 全局变量定义
int n,a,b;        // n:楼层总数, a:起始楼层, b:目标楼层
int k[201];       // 存储每层楼的数字k[i]
int v[201];       // 标记数组,记录楼层是否被访问过
int m=1e6;        // 存储最少按键次数,初始化为一个较大值
/*
深度优先搜索函数:
c:当前所在楼层
s:已按键次数
*/
void dfs(int c,int s){
    // 如果当前步数已大于等于最小值,无需继续搜索
    if(s>=m)return;
    // 如果到达达目标楼层,更新最小步数
    if(c==b){
        if(s<m)m=s;
        return;
    }
    v[c]=1;// 标记
    // 计算上移后的楼层
    int u=c+k[c];
    // 检查上移后的否合法且未访问过
    if(u<=n && v[u]==0){
        dfs(u,s+1);  // 递归访问上移后的楼层,步数+1
    }
    // 计算下移后的楼层
    int d=c-k[c];
    // 检查下移是否合法且未访问过
    if(d>=1 && v[d]==0){
        dfs(d,s+1);  // 递归访问下移后的楼层,步数+1
    }
    v[c]=0;// 回溯
}
int main(){
    cin>>n>>a>>b;
    // 特判 
    if(a==b){
        cout<<"0";
        return 0;
    }
    for(int i=1;i<=n;i++){
        cin>>k[i];
    }
    // 初始化访问标记数组
    for(int i=1;i<=n;i++){
        v[i]=0;
    }
    // 从起始楼层开始深度优先搜索,初始步数为0
    dfs(a,0);
    // 输出结果:若无法到达则输出-1,否则输出最小步数
    if(m==1e6)cout<<-1;
    else cout<<m;
    return 0;
}
