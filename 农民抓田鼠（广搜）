农民抓田鼠：

题目描述：
农田被田鼠大量破坏。农民知道了田鼠的位置，农民和田鼠都位于数轴上，农民起始位于点 N (0≤N≤100000)，田鼠位于点 K (0≤K≤100000)。农民有两种移动方式： 1、从 X 移动到 X-1 或 X+1 ，每次移动花费一分钟 2、从 X 移动到 2×X，每次移动花费一分钟 假设田鼠没有意识到农民的行动，站在原地不动。农民最少要花多少时间才能抓住田鼠？

输入格式：
两个正整数，N 和 K。农民和田鼠只在0~100000的范围内移动。

输出格式：
一个整数，农民抓到田鼠所要花费的最小分钟数。

样例：
输入数据 1：
5 20

输出数据 1：
2

提示：
数据范围与提示： 0≤K≤100000，0≤N≤100000

代码：
#include <bits/stdc++.h>
using namespace std;
struct node{
    int x,step;
}q[100001];
int a,b,front,back,v[100001];
void bfs(){
	q[back++]={a,0};
	v[a]=1;
	while(front<back){
		node t=q[front++];
		if(t.x==b){
			cout<<t.step;
			return;
		}
		for(int i=-1;i<=3;i+=2){
			int tmp=(i<=1?t.x+i:2*t.x);
			if(tmp>=0 && tmp<=100000 && v[tmp]==0){
				v[tmp]=1;
				q[back++]={tmp,t.step+1};
			}
		}
	}
}
int main(){
    cin>>a>>b;
    bfs();
    return 0;
}
