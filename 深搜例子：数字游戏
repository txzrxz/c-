数字游戏:

题目描述：
小雨作为一个数字迷，他经常把一个数字变成另外一个数字。现在他有一个正整数N，对N的任意一位数字可以执行以下2种操作:

1.将该位数字加1。若该位数字已经是9，加1之后变成0。

2.将该位数字减1。若该位数字已经是0，减1之后变成9。

现在总共可以执行1号操作不超过A次，2号操作不超过B次。那么，最大可以把N变成多少？

输入格式：
第一行1个正整数N。 第二行2个正整数A和B。

输出格式：
一个整数表示最大整数。

样例：
输入数据 1:
123
1 2

输出数据 1:
933

提示:
1<=N<=10^8,0<=A,B<=100

代码：
#include <bits/stdc++.h>
using namespace std;
int a,b;
string n,mx="0";
void dfs(int k){//当前处理 数字下标为k的这一位 
	if(k==n.size() || a==0 && b==0){
		mx=max(mx,n);
		return;
	}
	char c=n[k];//当前要处理的数字
	int num=c-'0';
	if(num==9)dfs(k+1);
	else{
		if(9-num<=a){//加法 能变成9 
			n[k]='9';
			a-=9-num;
			dfs(k+1);
			a+=9-num;
			n[k]=c;
		}else{//不能变成9 
			n[k]=c+a;
			a=0;
			dfs(k+1);
			a=n[k]-c;
			n[k]=c;
		}
		if(num+1<=b){//减法 变成9 
			b-=num+1;
			n[k]='9';
			dfs(k+1);
			b+=num+1;
			n[k]=c;
		}
	} 
} 
int main() {
	cin>>n>>a>>b;
	dfs(0);//从最高位 下标为0的位开始处理
	cout<<mx;
    return 0;
}
