因数：

题目描述：
给定A，I，求一个最小的N，使得N/A的商向上取整大于等于I。 向上取整的意思： 如23.3，向上取整得24； 如0.125，向上取整得1； 如3.91，向上取整得4。

输入格式：
一行两个整数A，I，用空格隔开（1≤A≤100，1≤I≤100）。

输出格式：
一行一个整数N。

样例：
输入数据 1：
38 24

输出数据 1：
875

代码：
#include <bits/stdc++.h>
using namespace std;
int main() {
	double a,i,num=2;
    cin>>a>>i;
    cout<<(i-1)*a+1<<endl;
    return 0;
}//公式算法
